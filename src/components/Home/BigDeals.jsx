import StudioContainer from "./StudioContainer"

const BigDeals = () => {
    return <StudioContainer title={"BIG DEALS"} paragraph={"This week's trending studio"} studio={[1, 2, 3, 4, 5]} />
}

export default BigDeals